<!DOCTYPE html>
<html>
<head>
  <title>Edit Data</title>
   <link href="../output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded shadow-md w-96">
    <h1 class="text-2xl font-semibold text-center mb-6">Edit Data Negara</h1>

    <form action="/update/<%= data.id %>" method="POST">
      <div class="mb-4">
        <label for="country" class="block text-gray-700 text-sm font-bold mb-2">Negara:</label>
        <input type="text" id="country" name="country" value="<%= data.country %>" class="w-full px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring focus:ring-blue-500">
        
        <% if (errors && errors.country) { %>
          <p class="text-red-500 text-xs mt-1"><%= errors.country %></p>
        <% } %>
        
      </div>

      <div class="mb-6">
        <label for="capital" class="block text-gray-700 text-sm font-bold mb-2">Ibukota:</label>
        <input type="text" id="capital" name="capital" value="<%= data.capital %>" class="w-full px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring focus:ring-blue-500">

        <% if (errors && errors.capital) { %>
          <p class="text-red-500 text-xs mt-1"><%= errors.capital %></p>
        <% } %>
      </div>

      <div class="flex justify-between">
        <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Simpan</button>
        <a href="/" class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Batal</a>
      </div>
    </form>
  </div>
</body>
</html>
